[workspace]
members = [
    "frantic-core",
    "frantic-cli",
    "frantic-web",
    "frantic-client",
    "frantic-updater",
]
resolver = "2"

# Reduces the binary size of the WASM file.
[profile.release]
lto = true
opt-level="z"
codegen-units = 1
panic = "abort"
